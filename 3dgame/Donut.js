class Food {
  constructor() {
    this.radius = gridSize / 3;
    this.angle = 0.01;
  }

  draw() {
    // this.angle += 0.01;
    // fill(255, 255, 0);
    // const [rotate, clearRotate] = useRotate(this.angle, this.angle, this.angle);
    // rotate();
    // noStroke();
    // texture(PUMPKIN_TEXTURE);
    // sphere(this.radius);
    // clearRotate();
  }
}
